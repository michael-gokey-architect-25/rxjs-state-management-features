// src/app/app.routes.ts
/**
 * Application Routes
 * 
 * This demo is a single-page application with no routing.
 * Routes array is empty but can be expanded for future features.
 */
import { Routes } from '@angular/router';

export const routes: Routes = [];
